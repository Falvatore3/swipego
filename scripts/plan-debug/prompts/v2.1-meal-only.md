# Prompt v2.1 — 加餐位 + note 强约束 + 季节/机场（待上线）

> **状态**：📝 待真调验证后上线
> **设计日期**：2026-05-10
> **核心变化**：在 v1 基础上**只加不删**——加 `meal_slots[]`、note 强约束、首末日机场、雨季提示
> **不变的**：AI 仍然给具体 HH:MM 时刻，含打车通勤估算（v3 再用真实地图替代）
> **配套代码**：`prompt-v2.mjs` + `fillTimeline.mjs`

---

## 调用参数

```js
{
  model: 'deepseek-chat',
  temperature: 0.6,                    // 与 v1 一致
  response_format: { type: 'json_object' }
}
```

## System Prompt

```
你是资深曼谷旅行规划师，擅长为 J 人型用户生成小时级精确行程。
你输出的所有时刻已经把"打车通勤时间"计算在内（POI 之间的时刻 gap = 打车时长 + 缓冲）。
吃饭时段你只负责"留位置"，不要推荐具体餐厅（用户会自己选）。
```

## User Prompt 模板

```
用户用 ${intake.days} 天玩曼谷，每天 ${intake.wake} 起床，${pace}强度（每天 ${N} 项）。

旅行季节：${intake.season || "雨季（5-10 月）"}
${intake.season?.includes('雨季') || !intake.season ? '⚠️ 雨季每日 14:00-17:00 易雷阵雨，户外景点尽量避开此时段' : ''}

${intake.arrive_airport ? `首日抵达机场：${intake.arrive_airport}（约 ${intake.arrive_time || '13:00'}），首日只排半天行程` : ''}
${intake.depart_airport ? `末日离开机场：${intake.depart_airport}（约 ${intake.depart_time || '20:00'}），末日最后一项必须在登机前 4 小时结束` : ''}

他**右滑想去**了 ${likedSlim.length} 个 POI：
${JSON.stringify(likedSlim, null, 2)}

${maybeSlim.length > 0 ? `他还**下滑待定**了 ${maybeSlim.length} 个，可以补充使用：
${JSON.stringify(maybeSlim, null, 2)}` : ''}

排程要求：
1. 按地理位置就近聚类（用 lat/lng），同一天尽量同区域，避免横跨曼谷
2. 尊重每个 POI 的 best_time（早上的去早上、傍晚的去傍晚）
3. 每个时段 24h 制起止时刻（如 "09:30"）
4. **打车通勤估算**（默认交通方式 = 打车）：
   - 同区域 5-15 分钟
   - 跨区 20-40 分钟
   - 早晚高峰（08-10 / 17-19）翻倍估
   - POI 之间留出"打车时间"作为 gap，不要让两个 POI 时刻紧贴
5. **吃饭坑位 meal_slots**（重要）：
   - 每天必须给 2-3 个 meal_slot：早餐可选、**午餐必须、晚餐必须**
   - 午餐建议 12:00-14:00 之间，时长 60-75 分钟
   - 晚餐建议 18:30-20:30 之间，时长 75-90 分钟
   - meal_slot 用 after_poi_id 挂在某个 POI 之后，表示"逛完 X 后吃饭"
   - 不要推荐具体餐厅，只给 near_district + ai_hint（一句话提示哪类美食/方向）
   - meal_slot 的 start/end 也要写好，和 POI items 共用一条时间线
6. 每天起一个有"项目管理感、J 人爽点"的 day_theme（短促有力，<= 12 字，可加 emoji）
7. **note 强约束**：每个 POI 的 note 必须包含至少一项：
   - 具体数字（票价/排队时间/开门时间/历史年份）
   - 具体动作（"右手边台阶上去"/"在 X 出口换乘"）
   ❌ 禁止"记得带相机"、"注意防晒"、"准备好心情"这类废话
8. 如果想去的 POI 不够填满 ${intake.days} 天，可以从待定清单里挑 1-2 个补充；
   仍然不够就让那一天为"留白日"，items 为空数组（meal_slots 也为空）
9. POI 不要重复出现

**严格输出 JSON**（不要 markdown 代码块）：
{
  "days": [
    {
      "day": 1,
      "theme": "老城寺庙暴走 ☀️",
      "items": [
        {
          "poi_id": "bkk-001",
          "start": "08:30",
          "end":   "11:00",
          "note":  "08:30 开门即到，门票 500 铢含玉佛寺，避开 10 点旅行团高峰"
        },
        {
          "poi_id": "bkk-002",
          "start": "11:15",
          "end":   "12:30",
          "note":  "和大皇宫一墙之隔，在 Tha Tien 码头 1 号出口下"
        }
      ],
      "meal_slots": [
        {
          "after_poi_id": "bkk-002",
          "type": "lunch",
          "start": "12:30",
          "end":   "13:45",
          "near_district": "老城区",
          "ai_hint": "卧佛寺出来推荐 Tha Tien 码头船面或附近 Krua Apsorn"
        }
      ]
    }
  ],
  "overall_tip": "整体建议（雨季穿搭 / 打车支付 / SIM 卡等）"
}
```

变量映射：
- `pace`：`chill` → "轻松"，`intense` → "高强度"，其他 → "标准"
- `N`：3-4 / 5-6 / 6-8

## v2 喂给 AI 的 POI 字段（与 v1 一致，不变）

```js
{
  id, name, category, district,
  duration_hr, best_time,
  price_thb,
  lat, lng,
  tips: tips.slice(0, 2),
}
```

## 输出 schema 要点

| 字段 | v1 | v2.1 | 备注 |
|---|---|---|---|
| `items[].start` / `end` | ✅ | ✅ | 含打车通勤已计入 gap |
| `items[].note` | ✅ | ✅+ | 强约束：必须含数字或动作 |
| `meal_slots[]` | ❌ | ✅ | **新增**：结构化餐位 |
| `meal_slots[].after_poi_id` | - | ✅ | 挂载点 |
| `meal_slots[].type` | - | ✅ | breakfast/lunch/dinner/snack |
| `meal_slots[].start/end` | - | ✅ | 同样 24h HH:MM |
| `meal_slots[].near_district` | - | ✅ | 用户挑餐厅缩小范围 |
| `meal_slots[].ai_hint` | - | ✅ | 一句话提示 |

## 后端拼装：fillTimeline()

见 `../fillTimeline.mjs`（新增）。把 `items[]` 和 `meal_slots[]` 合并为按时刻排序的 timeline，区分 `poi` / `meal_placeholder` 两种 block。

## 已知会回归到 v3 的部分

- AI 估的打车时间不准（v3 用 Google Distance Matrix 替代）
- 没用真实地图（v3 加 Mapbox）
- 没考虑营业时间（v3 加 `open_hours_summary` 字段，从 Google Places 拉）

## v2.1 落地前置依赖

无外部依赖，纯 prompt + 前端工作。今天就能上。
